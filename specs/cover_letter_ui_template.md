я хочу сделать так

сделать листинг "Фразы"

Сделать Раздел "Конструктор писем"

в конструкторе сделать так&#x20;

CRUD по сущности cover\\\_letter\\\_template

cover\\\_letter\\\_template = linked\\\_list\\\<letterItem>

{

has\\\_projects: bool,

value: linked\\\_list\\\<letterItem>

}



linked\\\_list \\\<letterItem>[

{&#x20;

type : opening, EXPERIENCE\\\_BRIDGE, PORTFOLIO\\\_INTRO ...

text

next:[

тут будет массив ссылок например id тех letterItem  которые я выберу&#x20;

]

}

]

в странице создать и в редактировать сделать консструктор с узлами (@xyflow/react) в котором я могу добавить узел  затем в поиске найти нужный letterItem по type и text

и соединить их в список&#x20;

также у cover\\\_letter\\\_template должен быть поле case это будет  select который юудет иметь значения на  случай

\- Если часть информации отсутствует — например, нет подходящих проектов,&#x20;
\- есть релевантная сфера
\- частичное совпадение&#x20;
\- нет подходящих проектов

чтобы потом при генерации система могла по этому case взять самый подходящий  шаблон



пока напиши мне spec для этой задачи

Есть важный момент модели: next: [] превращает структуру не в linked list, а в ориентированный граф вариантов. Это как раз подходит задаче: генератор идёт от стартового узла по одному из допустимых рёбер, поэтому бессмысленные сочетания исключаются. В spec я закреплю графовую модель, правила целостности и поведение при отсутствии подходящего шаблона. Первый и последний пункты про отсутствие проектов разведу как «портфолио пусто» и «проекты есть, но релевантных нет».



Read files

Архитектурно я разделю две сущности: «Фраза» хранит только переиспользуемые type + text, а переходы next принадлежат конкретному шаблону. Иначе редактирование связей одной и той же фразы одновременно изменяло бы все шаблоны, где она используется. В API и UI переходы будут представлены рёбрами графа, что напрямую совместимо с @xyflow/react.


суть в том что при выборе "Использовать шаблоны" у меня backend брал подходящий covert\_letter\_template например по case&#x20;
сделай мне spec с понятным шагами контрактами для этой задачи